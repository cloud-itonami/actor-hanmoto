{
  "name": "cloud-itonami-hanmoto",
  "private": true,
  "devDependencies": {
    "shadow-cljs": "3.4.6"
  },
  "scripts": {
    "build": "clojure -M:build",
    "deploy": "npm run build && wrangler deploy --config appview/hanmoto/wrangler.jsonc"
  }
}
